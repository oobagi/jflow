*.bak
.DS_Store
.env
.env.*
*.pem
*.key
*.p12
credentials.json
.playwright-mcp/

# Go
/jflow
/cmd/jflow/jflow
*.test
*.out

# Runtime jflow state at the repo root only (session logs, future workspace
# registries). Anchored to `/` so it doesn't shadow `internal/state/`.
/state/
# User preferences (DefaultDir, etc.). Lives next to /state/ at runtime.
/config.json
